{
  "name": "soundmintauction",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ soundmint-auction",
    "create-local": "graph create --node http://localhost:8020/ soundmint-auction",
    "remove-local": "graph remove --node http://localhost:8020/ soundmint-auction",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 soundmintauction"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}